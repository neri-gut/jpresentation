{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "operator-cap",
  "description": "Operator console: commands, windows, events. No microphone.",
  "windows": ["operator"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "opener:default",
    "operator-commands"
  ]
}
