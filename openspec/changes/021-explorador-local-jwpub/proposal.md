# Proposal: Explorador local y JWPUB

## Intent

Además de Esta Semana: raíces de carpeta del perfil y un inspector de `.jwpub`/`.epub` (documentos + Multimedia embebida o de catálogo). Stage sin copiar salvo “añadir a esta semana”.

## Scope

`multimedia` (+ parser ya descrito en `proveedor-medios`).
