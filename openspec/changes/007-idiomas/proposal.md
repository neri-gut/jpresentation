# Proposal: Idioma de UI vs idioma de contenido

## Intent

Separar traducción de JPresentation (JSON comunitarios, v1 `es`+`en`) del `langwritten` del catálogo JW (lista viva `alllangs=1`, semilla de lenguas frecuentes).

## Scope

`perfiles` + research `idiomas.md`. Fuera: ficheros `locales/*.json` de código.
