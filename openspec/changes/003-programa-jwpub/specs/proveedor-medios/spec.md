# Delta — Proveedor de medios

## ADDED Requirements

`PublicationCatalog`, `ScheduleParser`, `MediaResolver`. URI `jwpub-media://` como id interno. Extraer embebidos; descargar solo lo que no viene en el paquete.
