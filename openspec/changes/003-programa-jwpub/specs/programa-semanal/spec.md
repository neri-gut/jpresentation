# Delta — Programa semanal

## ADDED Requirements

Semana como unidad, parser enchufable alineado a `loadPub`, canciones como número, `MediaRef` (embedded / jwpub-media / catalog / local), fallos parciales, persistencia por perfil+fecha+idioma.
