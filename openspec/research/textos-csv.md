# CSV de textos

```
reference,label,note
Pr 3:5-6,Confía en Jehová,párrafo 8
1Ti 3:16,Misterio sagrado,
```

Opcional `text` si el operador ya trae la redacción. Si falta, `BibleSource.lookup(reference)`.
